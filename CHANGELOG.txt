HDC BETA API CHANGELOG
======================

2026-08-31 - Build 24A customer requests and technician discovery redesign
- Adds a guided Customer request intake, review, list, and record experience
  backed by the existing request, proposal, and transaction providers.
- Keeps all offers reachable and exposes an accepted service workspace through
  responsive action panels that do not overflow on compact screens.
- Redesigns Technician discovery around approved public profiles, textual area
  matches, and technician-stated information without synthetic distance or
  reputation claims.
- Adds no API, database, authorization, payment, document, dispute, or chat
  behavior changes.

2026-08-27 - Build 20 workflow refresh, proposal reliability, and private chat
- Refreshes all account-authorized requests, proposals, and transactions when
  Customers open their request list, request details, or proposal inbox.
- Prevents proposal autosave from racing submission, surfaces failures, and
  safely replays a canonical proposal after a lost response.
- Moves accepted-transaction chat from device-local preferences to protected
  backend storage visible only to the authenticated Customer and Technician.
- Adds message moderation, read state, a bounded beta quota, cross-account
  cache guards, polling/manual refresh, migration 0010, and contract tests.

2026-08-26 - Build 19 live technician directory and explicit opportunities
- Gives active Technicians a dedicated opportunity feed containing open
  requests posted by other accounts, independent of the general workflow
  bootstrap cache.
- Replaces the Find a Technician placeholder with manual profile search,
  service-area ordering, public contact cards, and OpenStreetMap area links.
- Lists only active, approved Technician profiles that explicitly enabled
  public discovery; private profiles, account UUIDs, and internal roles stay
  private.
- Adds a visible Technician Jobs dashboard action and clears in-flight
  discovery state safely whenever the signed-in account changes.

2026-08-25 - Build 18 live technician request discovery
- Refreshes the technician opportunity feed on entry, pull-to-refresh, and the
  explicit refresh action so separately logged-in accounts receive new posts.
- Adds location/service/keyword search, sort controls, Technician-profile area
  matching, and configurable external map search without pretending textual
  areas are exact GPS distances.
- Prevents customer records from leaking into another account's My Requests
  view and blocks an account from proposing to its own request.
- Synchronizes Flutter, web, API health, dashboard, and package release markers
  and verifies them automatically before deployment.

2026-08-24 - Build 17 service-request reliability hotfix
- Repaired the Neon/PostgreSQL role membership that prevented the API from
  entering HDC's restricted RLS role for service requests and proposals.
- Added migration 0009 plus readiness validation for workflow authority.
- Made request publishing retry-safe with stable identifiers, coalesced submit
  taps, and idempotent backend creation.
- Added safe request references and actionable client errors while keeping
  sensitive payloads and database details out of responses and logs.
- Added an opt-in isolated-branch integration test for the authenticated
  request-publication path.

2026-08-24 - Build 16 secure Netlify web and API deployment foundation
- Consolidated Flutter web, password recovery, and API routes under one HTTPS
  origin so browser authentication does not require broad CORS access.
- Added pinned Flutter web build checks, an enforced dependency lockfile, and
  same-origin runtime API discovery.
- Added hardened static/API headers and a provider-portable deployment,
  preview, promotion, and rollback runbook.
- Self-hosted Flutter renderer and font resources so Chrome loads Build 16
  without relaxing the HDC-only content-security policy for third-party CDNs.
- Rehearsed migrations 0006-0008 on an isolated Neon branch, then promoted the
  identical migration transaction to production after explicit approval.
- Verified production migrations 0001-0008, required RLS/triggers, the single
  active Owner, and both existing accounts as distinct UUIDs; retained a
  pre-change rollback branch.
- Updated four transitive packages to Flutter 3.47.1's required pins and
  verified every final hosted package archive against its lockfile SHA-256.

2026-08-24 - Build 15 product marketplace and Flutter web authentication
- Added Shop Technology to the dashboard and app bar with a public catalog,
  search/category filters, and account-scoped My Purchases history.
- Added buyer purchase requests and cancellation plus seller accept/decline
  order management in Items & Sales.
- Seller acceptance atomically allocates stock but is not payment, receipt,
  delivery, or fulfillment proof.
- Added migration 0008 with UUID idempotency, participant isolation, lifecycle
  evidence, version conflicts, integer minor-unit totals, and self-buy blocking.
- Fixed Chrome API access with restricted CORS and OPTIONS preflight support;
  hosted Flutter web origins must be listed in HDC_WEB_ALLOWED_ORIGINS.
- No production migration or backend deployment was performed by this package.

2026-08-24 - Build 14 marketplace items and sales dashboard
- Added a dashboard Items & Sales card with Selling, Sold, and Low Stock counts.
- Added approved Seller, Supplier, and Store listing management with independent
  role-profile ownership under one authenticated account UUID.
- Added draft, active, paused, sold, and archived states with version conflicts,
  lifecycle evidence, stock validation, and integer minor-unit PHP pricing.
- Added migration 0007 and automatic pausing when a selling role is removed.
- Kept prior listing history visible but read-only after role removal and
  rejected cross-account listing payloads on the client.
- Kept seller-reported sold state separate from future verified payment,
  receipt, fulfillment, and dispute systems.
- No production migration or deployment was performed by this package.

2026-08-24 - Build 13 provider portability and recovery foundation
- Centralized runtime environment access and exported the HDC Web API handler
  so alternate Node-compatible hosting adapters can reuse domain behavior.
- Added PostgreSQL portability contracts, migration ledger, external-provider
  reference isolation, dependency readiness, encrypted backup verification,
  resumable storage checkpoints, and versioned data-export manifests.
- Added provider-neutral email/SMS, object-storage, and payment contracts with
  disabled, queued, or fail-closed defaults; no paid provider was activated.
- Separated session-signing and recovery-pepper keys and preserved explicit
  previous/legacy verification paths for safe key rotation.
- Added CI boundary enforcement and the provider migration/rollback runbook.
- Added normal, read-only, maintenance, and incident operation gates that do
  not depend on a hosting provider dashboard.
- No production database, hosting, DNS, or paid-service change was performed.

2026-08-22 - Build 12 structured role applications and secure recovery
- Replaced free-text elevated public-role requests with versioned,
  role-specific required fields, private applicant snapshots, request-changes,
  scoped internal review grants, and auditable role-grant provenance.
- Added three required registration recovery questions. Answers are normalized,
  server-peppered, bcrypt-hashed, and never returned or logged in plaintext.
- Added rate-limited recovery verification: any two correct answers authorize a
  15-minute one-time reset credential; all prior sessions are revoked after use.
- Added the private manual recovery queue for Owner, Super Admin, or explicitly
  granted reviewers without exposing answer match counts.
- Preserved the existing owner-issued password reset token contract and web
  reset link through a compatibility endpoint.
- Added public member IDs separate from authentication UUIDs, terms acceptance
  evidence, public-role lifecycle provenance, organization memberships, and
  store-branch foundations. Organization credentials are never shared.
- Rehearsed migration 0005 twice on an isolated Neon branch; both current users,
  their unique UUIDs/public IDs, and the existing reset token were preserved.
- Backend 0.6.0 passes strict TypeScript compilation and all 32 tests.
- Promoted migration 0005 to production after the deploy preview and CI passed;
  production still has two distinct account UUIDs, two distinct public member
  IDs, one active Owner, and the existing reset token.
- Deployed Backend 0.6.0 to Netlify production. The live health and recovery-
  question routes returned 200, the reset page returned 200, and the private
  dashboard continued to reject unauthenticated access with 401.

2026-08-22 - Build 10 production rollout and active-auth compatibility
- Added root migration 0001 for the active Neon auth contract. It validates
  existing role values, preserves all users and assignments, and converts the
  platform-role column from constrained text to hdc_account_role.
- Kept the obsolete Supabase adapter migration isolated from the active Neon
  deployment path.
- Rehearsed migrations 0001-0004, profile backfill, row-level security, Owner
  bootstrap, audit logging, and dashboard statistics on an isolated branch.
- Applied the verified sequence atomically to production and created exactly
  one active Owner through a guarded audited bootstrap.
- Deployed Backend 0.5.0 to the existing HDC Netlify production site. The live
  health route returned 200, while unauthenticated internal, role, and profile
  routes returned 401.
- Retained the pre-change Neon rollback branch. No GitHub remote was changed.

2026-08-21 - Flutter 0.6.4+10 private operations dashboard
- Made the public Role Center and profile flow platform-only; neither public
  API overview returns internal authority, staff assignments, permissions, or
  private queue counts.
- Added a separate private operations dashboard and public/private switch that
  exists only for authenticated accounts with backend-hydrated private access.
- Private dashboard data is loaded only after the authorized workspace opens;
  it is not prefetched into the public dashboard.
- Added permission-filtered statistics, private staff assignments, sanitized
  operational activity, and authorized-tool indicators.
- Moved platform-role application review behind the private provider and
  server-authorized internal endpoints.
- Added GET /api/internal/dashboard and bumped the matching API contract to
  0.5.0 without adding a new private-dashboard feature table.
- Strict TypeScript compilation and all 27 backend tests pass. Flutter/Dart SDK
  execution remains a clean-extraction release gate outside this workspace.
- The verified production rollout is recorded in the 2026-08-22 entry above.

2026-08-21 - Flutter 0.6.4+9 analyzer cleanup and connection rescan
- Removed the unused HDC profile-model import reported by Flutter analyze.
- Replaced the obsolete double-underscore callback parameter with Dart's
  wildcard identifier form.
- Preserved the requested Role Center profile until refreshed account roles are
  available, so a newly approved role opens its intended workspace profile.
- Rechecked all 303 project/test Dart files and 828 import/export/part
  directives; no missing local source reference was found.
- Re-ran strict backend TypeScript compilation and all 23 automated tests.
- Excluded machine-local Flutter caches, ephemeral plugin trees, IDE files, and
  generated registrants from the corrected source package.

2026-08-21 - Sprint 6.4G one-account role profiles
- Added one shared member profile per HDC user account.
- Added one independently editable profile per active Customer, Technician,
  Business, Seller, Supplier, or Store role under the same user ID.
- Added authenticated profile overview, shared-member update, and role-profile
  update endpoints; inactive roles cannot create or edit profiles.
- Added strict server validation for common public fields and every role's
  specific details, including bounded lists, contact data, URLs, and numbers.
- Added a private internal-staff view assembled from internal roles and
  department/section assignments without creating public staff identities.
- Added migration 0004 with backfill, profile constraints, indexes, versioning,
  row-level-security enablement, and denied public table access.
- Added five profile tests; strict TypeScript compilation and all 23 backend
  tests pass locally.
- Production database, deployment, and GitHub remote remain unchanged pending
  isolated-branch migration and preview verification.

2026-08-21 - Initial authentication backend foundation
- Connected the HDC backend repository for direct managed writes.
- Added Netlify Functions project configuration.
- Added provider-neutral server environment handling.
- Added Neon PostgreSQL database adapter boundary.
- Added JSON/authorization helpers and authentication input validation.
- Added signed, revocable session-token handling.
- Added /api/health.
- Added customer-only public registration.
- Added login with backend-owned account status and role lookup.
- Added failed-login throttling backed by the HDC security audit log.
- Added session restoration/validation.
- Added logout with backend session revocation.
- Added security auditing for registration, login, and logout flows.
- Added validation tests, environment template, README, and TypeScript configuration.
- Confirmed no real database URL, session secret, or beta-account password is committed to this repository.

2026-08-21 - Netlify deployment link
- User linked the Netlify hdc-beta-api project to this GitHub repository.
- Added this changelog commit after the link to trigger continuous deployment from main.

2026-08-21 - Flutter auth contract hardening
- Added backend-owned account createdAt and updatedAt values to auth user responses so Flutter does not invent technician membership dates.
- Tightened timestamp serialization for strict TypeScript compatibility.
- Netlify continuous deployment republished the production auth function successfully after the API contract update.

2026-08-21 - Netlify runtime configuration recovery
- Investigated live Flutter registration/login failures returning the generic temporary-auth-unavailable message.
- Confirmed the Flutter build is wired to HdcApiAuthGateway and the production Netlify API URL.
- Restored the Neon DATABASE_URL in the Netlify project as a server-only runtime/function environment value.
- Triggered a fresh production deployment so the serverless function reloads the corrected runtime configuration.
- Kept privileged backend credentials out of Flutter source and this repository.

2026-08-21 - Production session signing recovery
- Added a new protected HDC_SESSION_SECRET to the Netlify production Functions/Runtime environment after diagnostics confirmed it was missing.
- No signing secret value was written to source, logs, or documentation.
- Triggered continuous deployment from main so the production function reloads the restored signing configuration.

2026-08-21 - Auth diagnostic cleanup
- Confirmed the live diagnostic reported database configuration, signing configuration, database reachability, and auth schema readiness as healthy.
- Removed the temporary public auth diagnostic endpoint after verification.
- Kept /api/health as the normal non-sensitive service health endpoint.

Pending verification before public beta:
- Complete live client registration/login/session/logout test from the integrated Flutter build.
- Provision the approved beta customer/technician test identities through the backend flow without committing passwords.

2026-08-21 - Sprint 6.4C/D workflow authority
- Added the additive 0002 workflow migration for service requests, proposals,
  transaction seeds, and service transactions.
- Added a restricted NOLOGIN application role, request-local identity/role
  context, row-level-security policies, relationship constraints, indexes,
  lifecycle timestamps, versioning, and server-maintained offer counts.
- Added authenticated bootstrap and mutation endpoints for requests, proposals,
  atomic proposal acceptance, and participant-authorized transaction status.
- Proposal acceptance now closes competing proposals and creates the consumed
  handoff plus confirmed transaction workspace in one database transaction.
- Identity, reputation, quality score, timestamps, activity history, and
  customer/technician relationships are derived or verified by the backend.
- Standardized request-before-proposal locking to prevent competing acceptance
  and proposal-update deadlocks.
- Cancelling an editable request now expires technician drafts and declines
  active proposals in the same database transaction.
- Added eight workflow validation/authorization tests; the backend suite now
  passes 12 tests and strict TypeScript compilation.
- Production schema and deployment remain intentionally unchanged until the
  migration passes on an isolated Neon branch and preview workflow tests pass.

2026-08-21 - Sprint 6.4F platform/internal role separation
- Split user-facing platform roles from private HDC internal authority in the
  authentication response and authorization helpers.
- Added Supplier while retaining the existing Store workspace compatibility
  role; every non-Customer platform role now uses the approval workflow.
- Added the Owner -> Super Admin -> Admin hierarchy and community-scoped
  Moderator without exposing internal roles to public registration.
- Added role application, Owner/Super Admin review, approval-note, and
  high-priority role-notification endpoints.
- Added migration 0003 for internal assignments, platform role applications,
  notifications, and configurable internal departments/sections.
- Migrated legacy Admin/Super Admin values out of the platform-role table while
  preserving compatibility during rollout.
- Added six role-domain tests; strict TypeScript compilation and all 18 backend
  tests pass locally.
- Production database, deployment, and GitHub remote remain unchanged pending
  isolated-branch migration and preview verification.

2026-08-22 - Flutter 0.6.4+11 live-data and account isolation
- Removed visible demo dashboard, technician, marketplace, review, simulator,
  and sequential ticket fixtures.
- Scoped dashboard data, local tickets, and saved marketplace state to the
  authenticated UUID and protected workflow refreshes from stale accounts.
- Added UUID response validation, account-identifying dashboard details, and
  account-switch regression coverage.

2026-08-24 - HDC 0.6.4+12 account security and structured role applications
- Added mandatory three-question account recovery and versioned Terms/Privacy
  acceptance for every new registration.
- Added 2-of-3 Forgot Password verification, single-use reset authorization,
  manual private review, reset throttling, security audit, and session revocation.
- Added authenticated recovery-answer setup for accounts created before Build 12.
- Added role-specific application forms and private approve/request-changes/reject
  review for Technician, Business, Seller, Supplier, and Store.
- Preserved one unique UUID account per person and kept internal roles private.
- Removed obsolete top-level Appwrite setup files and saved test credentials.
