name: Build 24.2 Portability Fix

on:
  push:
    branches:
      - codex/build24-2-location-role-controls
    paths:
      - scripts/build24_2_portability.py
      - .github/workflows/build24-2-portability.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/build24-2-location-role-controls
          fetch-depth: 0
      - run: python3 scripts/build24_2_portability.py
      - name: Commit portability fix
        shell: bash
        run: |
          rm -f scripts/build24_2_portability.py .github/workflows/build24-2-portability.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Build 24.2: preserve provider portability in standalone functions"
          git push origin HEAD:codex/build24-2-location-role-controls
